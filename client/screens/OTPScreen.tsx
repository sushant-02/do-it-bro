import { StyleSheet, Text, View } from 'react-native';

const OTPScreen = () => {
	return (
		<View>
			<Text>OTPScreen</Text>
		</View>
	);
};

export default OTPScreen;

const styles = StyleSheet.create({});
